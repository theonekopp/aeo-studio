# Option A: Use server-side proxy with internal URL (recommended on Railway)
#API_BASE_INTERNAL=https://<api-service>.railway.internal

# Option B: Call API directly from browser (requires public API URL)
#NEXT_PUBLIC_API_BASE=https://<api-service>.up.railway.app
